<?xml version="1.0"?>
<ruleset name="lockbox-gradle-plugins-pmd-test-rules"
         xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0
                             https://pmd.sourceforge.io/ruleset_2_0_0.xsd">

    <description>
        PMD ruleset for unit and integration tests in j-standardizer.
        Mirrors the main ruleset but relaxes rules that are overly restrictive for test code.
    </description>

    <!-- Best Practices (test-relaxed) -->
    <rule ref="category/java/bestpractices.xml">
        <!-- Lombok generates these -->
        <exclude name="AccessorMethodGeneration"/>
        <!-- Allow builders with many parameters -->
        <exclude name="AvoidReassigningParameters"/>
        <!-- JUnit 5 doesn't require public -->
        <exclude name="JUnit5TestShouldBePackagePrivate"/>
        <!-- SLF4J guards are enforced by Checkstyle; PMD check is redundant -->
        <exclude name="GuardLogStatement"/>
        <!-- Tests may verify behaviour without an assertion (e.g. exception-expected) -->
        <exclude name="UnitTestShouldIncludeAssert"/>
        <!-- Tests often benefit from thorough, multi-step assertions -->
        <exclude name="UnitTestContainsTooManyAsserts"/>
        <!-- AssertJ provides self-describing assertions; message parameter adds noise -->
        <exclude name="UnitTestAssertionsShouldIncludeMessage"/>
        <!-- TestContainers and fixture data legitimately use hard-coded IPs -->
        <exclude name="AvoidUsingHardCodedIP"/>
    </rule>

    <!-- Code Style (test-relaxed) -->
    <rule ref="category/java/codestyle.xml">
        <!-- Lombok handles these -->
        <exclude name="AtLeastOneConstructor"/>
        <exclude name="CommentDefaultAccessModifier"/>
        <exclude name="OnlyOneReturn"/>
        <!-- Allow short variable names in lambdas and loops -->
        <exclude name="ShortVariable"/>
        <exclude name="LongVariable"/>
        <!-- Allow descriptive method names even if long -->
        <exclude name="ShortMethodName"/>
        <!-- Class naming is fine -->
        <exclude name="ClassNamingConventions"/>
        <!-- Domain model convention: allow short class names (User, Flag, Form, Page) -->
        <exclude name="ShortClassName"/>
        <!-- Allow for local variables in certain contexts -->
        <exclude name="LocalVariableCouldBeFinal"/>
        <exclude name="MethodArgumentCouldBeFinal"/>
        <!-- Tests commonly use many static imports (AssertJ, Mockito, etc.) -->
        <exclude name="TooManyStaticImports"/>
        <!-- Tests legitimately repeat string literals across test cases -->
        <exclude name="AvoidDuplicateLiterals"/>
    </rule>

    <!-- Design (test-relaxed) -->
    <rule ref="category/java/design.xml">
        <!-- Allow complex builders and DTOs -->
        <exclude name="DataClass"/>
        <exclude name="TooManyMethods"/>
        <exclude name="ExcessiveImports"/>
        <!-- Lombok generates these -->
        <exclude name="LawOfDemeter"/>
        <!-- Allow reasonable complexity -->
        <exclude name="CyclomaticComplexity"/>
        <exclude name="NPathComplexity"/>
        <exclude name="CognitiveComplexity"/>
        <!-- Spring requires non-final classes for CGLIB proxying -->
        <exclude name="ClassWithOnlyPrivateConstructorsShouldBeFinal"/>
        <!-- Test classes naturally depend on many collaborators -->
        <exclude name="CouplingBetweenObjects"/>
        <!-- Tests legitimately throw RuntimeException to simulate failures -->
        <exclude name="AvoidThrowingRawExceptionTypes"/>
    </rule>
    <rule ref="category/java/design.xml/TooManyFields">
        <properties>
            <property name="maxfields" value="20"/>
        </properties>
    </rule>

    <!-- Documentation -->
    <rule ref="category/java/documentation.xml">
        <!-- We use Checkstyle for Javadoc -->
        <exclude name="CommentSize"/>
        <exclude name="CommentRequired"/>
    </rule>

    <!-- Error Prone -->
    <rule ref="category/java/errorprone.xml">
        <!-- Allow null returns in certain cases -->
        <exclude name="ReturnEmptyCollectionRatherThanNull"/>
        <!-- Allow assigning null -->
        <exclude name="NullAssignment"/>
        <!-- Allow test classes to have only one method -->
        <exclude name="TestClassWithoutTestCases"/>
        <!-- Final utility classes don't need static methods -->
        <exclude name="MissingStaticMethodInNonInstantiatableClass"/>
        <!-- Lombok immutable patterns: field names matching method names -->
        <exclude name="AvoidFieldNameMatchingMethodName"/>
        <!-- Allow field names matching type names (e.g., Page.page) -->
        <exclude name="AvoidFieldNameMatchingTypeName"/>
        <!-- Spring beans and JPA entities are not Serializable by design -->
        <exclude name="NonSerializableClass"/>
    </rule>

    <!-- Multithreading -->
    <rule ref="category/java/multithreading.xml">
        <!-- Spring manages thread safety -->
        <exclude name="UseConcurrentHashMap"/>
        <exclude name="DoNotUseThreads"/>
    </rule>

    <!-- Performance -->
    <rule ref="category/java/performance.xml">
        <!-- Allow String instantiation for clarity -->
        <exclude name="AvoidInstantiatingObjectsInLoops"/>
    </rule>

    <!-- Security -->
    <rule ref="category/java/security.xml"/>

</ruleset>
